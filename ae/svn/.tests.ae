import build
import bash
import bash (jobs)

main() {
    b = build.start()
    build.dep(b, "ae/svn")
    build.dep(b, "ae/svnadmin")
    build.dep(b, "ae/svnserver")
    bash.test(b) {
        jobs(8)
    }
}
