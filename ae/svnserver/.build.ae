import build
import aether
import aether (source, output)

main() {
    b = build.start()
    aether.program(b) {
        source("main.ae")
        output("aether-svnserver")
    }
    aether.program(b) {
        source("seed.ae")
        output("svnae-seed")
    }
}
