import build
import bash
import bash (script, pre_command, post_command)
import svnae
import svnae (svn_server)

main() {
    b = build.start()
    build.dep(b, "ae/svn")
    build.dep(b, "ae/svnserver")
    bash.test(b) {
        svn_server("test_mergeinfo_arith", "9510")
        script("test_mergeinfo_arith.sh")
    }
}
