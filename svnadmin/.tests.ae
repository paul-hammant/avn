import build
import bash
import bash (jobs)

main() {
    b = build.start()
    build.dep(b, "svnadmin")
    build.dep(b, "svnserver")
    build.dep(b, "svnserver/.build-seed.ae")
    build.dep(b, "svn")
    bash.test(b) {
        jobs(8)
    }
}
