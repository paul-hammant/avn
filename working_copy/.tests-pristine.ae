import build
import aether
import aether (source, output, regen, link_flag, regen_with)

main() {
    b = build.start()
    aether.program_test(b) {
        source("test_pristine.ae")
        output("test_wc_pristine")
        link_flag("-laether_sqlite")
        link_flag("-lsqlite3")
        link_flag("-lz")
        link_flag("-lssl")
        link_flag("-lcrypto")
        regen_with("../working_copy/module.ae", "fs,os,net")
        regen_with("../repo_storage/module.ae", "fs,os")
        regen_with("../repos/module.ae", "fs,net,os")
        regen_with("../client/module.ae", "fs,os,net")
        regen_with("../util/module.ae", "fs")
        regen("../ffi/openssl/module.ae")
    }
}
